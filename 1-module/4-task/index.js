/**
 * checkSpam
 * @param {string} str base
 * @returns {boolean}
 */
function checkSpam(str) {
  if (str.includes("1XbeT") || str.includes("XXX"))  {
    return true;
  }
  else () {
   return false;
}
